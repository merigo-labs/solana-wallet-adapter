/// Call Methods
/// ------------------------------------------------------------------------------------------------

/// Method channel call names invoked by the native platform (e.g. Android, iOS).
enum CallMethod {
  openStore,
  openWallet,
  closeWallet,
  walletClosed,
  ;
}